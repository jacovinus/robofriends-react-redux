import React from 'react';

const ScrollContact = (props) =>{
    return (
        <div style={{border: '3px solid lightblue', height: '600px;'}}>
            {props.children}
        </div>
    )
}
export default ScrollContact;
